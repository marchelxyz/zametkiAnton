# 📝 Telegram Mini App - Заметки

Мини-приложение для Telegram, позволяющее создавать и управлять заметками. Готово к развёртыванию на Railway с PostgreSQL.

## 🚀 Развёртывание на Railway

### Шаг 1: Создание проекта на Railway

1. Перейдите на [railway.app](https://railway.app) и войдите в аккаунт
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"** и подключите этот репозиторий

### Шаг 2: Добавление PostgreSQL

1. В проекте нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway автоматически создаст базу данных и добавит переменную `DATABASE_URL`

### Шаг 3: Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте полученный **API Token**

### Шаг 4: Настройка переменных окружения

В Railway, в настройках вашего сервиса, добавьте переменные:

| Переменная | Описание |
|------------|----------|
| `BOT_TOKEN` | Токен вашего Telegram бота |
| `DATABASE_URL` | Автоматически добавляется Railway при подключении PostgreSQL |

### Шаг 5: Настройка Mini App в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots` → выберите вашего бота
3. Нажмите **"Bot Settings"** → **"Menu Button"** → **"Configure Menu Button"**
4. Отправьте URL вашего приложения на Railway (например: `https://your-app.up.railway.app`)
5. Укажите название кнопки (например: "📝 Заметки")

### Шаг 6: Запуск

После настройки Railway автоматически развернёт приложение. Откройте бота в Telegram и нажмите на кнопку меню!

---

## 🛠 Локальная разработка

### Требования

- Python 3.9+
- PostgreSQL (или используйте SQLite для разработки)

### Установка

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd telegram-notes-app

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл
echo "BOT_TOKEN=your_bot_token_here" > .env
echo "DATABASE_URL=postgresql://user:password@localhost:5432/notes_db" >> .env

# Запустите приложение
python app.py
```

---

## 📁 Структура проекта

```
├── app.py              # Основной Flask сервер
├── database.py         # Работа с PostgreSQL (SQLAlchemy)
├── templates/
│   └── index.html      # HTML/CSS/JS для Mini App
├── requirements.txt    # Python зависимости
├── Procfile           # Конфигурация для Railway
├── railway.toml       # Настройки Railway
└── README.md          # Документация
```

---

## 🔧 API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/` | HTML страница Mini App |
| GET | `/api/notes` | Получить все заметки пользователя |
| POST | `/api/notes` | Создать новую заметку |
| GET | `/api/notes/<id>` | Получить заметку по ID |
| PUT | `/api/notes/<id>` | Обновить заметку |
| DELETE | `/api/notes/<id>` | Удалить заметку |
| GET | `/health` | Health check для Railway |

---

## 🔐 Безопасность

- Все API запросы проверяют `initData` от Telegram для аутентификации пользователя
- Каждый пользователь видит только свои заметки
- Данные передаются по HTTPS

---

## 📝 Лицензия

MIT License
