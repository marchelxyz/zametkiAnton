# 📝 Telegram Mini App - Заметки и Задачи

Мини-приложение для Telegram, позволяющее создавать заметки и задачи с периодическими напоминаниями. Готово к развёртыванию на Railway с PostgreSQL.

## ✨ Возможности

- 📝 **Заметки** — создание, редактирование и удаление заметок
- ⏰ **Задачи с напоминаниями** — создание задач с выбором интервала уведомлений
- 🔔 **Telegram уведомления** — бот отправляет напоминания через заданные интервалы
- ⏸ **Управление уведомлениями** — возможность приостановить/возобновить напоминания
- 🎨 **Адаптивный дизайн** — автоматическая подстройка под тему Telegram

## 🚀 Развёртывание на Railway

### Шаг 1: Создание проекта на Railway

1. Перейдите на [railway.app](https://railway.app) и войдите в аккаунт
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"** и подключите этот репозиторий

### Шаг 2: Добавление PostgreSQL

1. В проекте нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway автоматически создаст базу данных и добавит переменную `DATABASE_URL`

### Шаг 3: Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте полученный **API Token**

### Шаг 4: Настройка переменных окружения

В Railway, в настройках вашего сервиса, добавьте переменные:

| Переменная | Описание |
|------------|----------|
| `BOT_TOKEN` | Токен вашего Telegram бота (обязательно для уведомлений) |
| `DATABASE_URL` | Автоматически добавляется Railway при подключении PostgreSQL |

### Шаг 5: Настройка Mini App в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots` → выберите вашего бота
3. Нажмите **"Bot Settings"** → **"Menu Button"** → **"Configure Menu Button"**
4. Отправьте URL вашего приложения на Railway (например: `https://your-app.up.railway.app`)
5. Укажите название кнопки (например: "📝 Заметки")

### Шаг 6: Запуск

После настройки Railway автоматически развернёт приложение. Откройте бота в Telegram и нажмите на кнопку меню!

---

## 🛠 Локальная разработка

### Требования

- Python 3.9+

### Установка

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd telegram-notes-app

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл из шаблона
cp .env.example .env

# Запустите приложение
python app.py
```

### Режимы работы

**Режим разработки (DEBUG=true):**
- Не требуется Telegram бот или авторизация
- Приложение работает в браузере по адресу http://localhost:5000
- Используется SQLite база данных (файл `notes_app.db`)
- Все заметки и задачи сохраняются локально

**Режим Production (DEBUG=false):**
- Требуется валидный `BOT_TOKEN` от @BotFather
- Приложение работает только через Telegram Mini App
- Рекомендуется использовать PostgreSQL

---

## 📁 Структура проекта

```
├── app.py              # Основной Flask сервер с планировщиком
├── database.py         # Работа с PostgreSQL (SQLAlchemy)
├── templates/
│   └── index.html      # HTML/CSS/JS для Mini App
├── requirements.txt    # Python зависимости
├── Procfile           # Конфигурация для Railway
├── railway.toml       # Настройки Railway
└── README.md          # Документация
```

---

## 🔧 API Endpoints

### Заметки

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/` | HTML страница Mini App |
| GET | `/api/notes` | Получить все заметки пользователя |
| POST | `/api/notes` | Создать новую заметку |
| GET | `/api/notes/<id>` | Получить заметку по ID |
| PUT | `/api/notes/<id>` | Обновить заметку |
| DELETE | `/api/notes/<id>` | Удалить заметку |

### Задачи

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/tasks` | Получить все задачи пользователя |
| POST | `/api/tasks` | Создать новую задачу с напоминанием |
| GET | `/api/tasks/<id>` | Получить задачу по ID |
| PUT | `/api/tasks/<id>` | Обновить задачу |
| DELETE | `/api/tasks/<id>` | Удалить задачу |
| POST | `/api/tasks/<id>/toggle` | Включить/выключить уведомления |

### Система

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/health` | Health check для Railway |

---

## ⏰ Система уведомлений

Приложение использует APScheduler для проверки задач каждую минуту. Когда наступает время уведомления:

1. Бот отправляет сообщение пользователю в Telegram
2. Время следующего уведомления автоматически обновляется
3. Цикл повторяется пока задача активна

### Поддерживаемые интервалы:
- **Минуты** — от 1 до 999 минут
- **Часы** — от 1 до 999 часов
- **Дни** — от 1 до 7 дней

---

## 🔐 Безопасность

- Все API запросы проверяют `initData` от Telegram для аутентификации пользователя
- Каждый пользователь видит только свои заметки и задачи
- Данные передаются по HTTPS

---

## 📝 Лицензия

MIT License
