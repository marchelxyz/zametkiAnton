[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -c 'from database import init_db; init_db()' && gunicorn app:app --bind 0.0.0.0:$PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
